#HW4 Problem 1
#Conjecture: y is prime infinitely often when n is in Z
#y = x ** 2 + x + 1

for i in range(-100000, 100000):
    y = i ** 2 + i + 1
    if y.is_prime():
        print str(i) + " results in the prime number " + str(y)
